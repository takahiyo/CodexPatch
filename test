PR作成の簡易マニュアル

1. 変更内容を確認し、必要に応じてファイルを編集します。
2. `git status` で変更を確認し、`git add` でステージングします。
3. `git commit -m "メッセージ"` でコミットします。
4. 必要に応じてテストやチェックを実行します。
5. `make_pr` ツールを使い、タイトルと本文を指定してPRを作成します。
6. PR内容を再確認し、問題がなければ提出します。
